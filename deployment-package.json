{
  "name": "hangout-bar-deployment",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
